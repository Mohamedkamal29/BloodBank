<resources>

                     <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
                   <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

                         <!-- No Actionbar -->
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>


                           <!-- EditText -->
    <style name="edittextRegistrationTheme" parent="@android:style/Widget.EditText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/authorization_edittext_shape</item>
        <item name="android:padding">10dp</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/dark_red</item>
        <item name="android:textColorHint">@color/dark_red</item>
    </style>

                    <!-- Spinner -->
    <style name="spinnerRegistrationTheme" >
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@drawable/authorization_edittext_shape</item>
        <item name="android:padding">10dp</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/dark_red</item>
        <item name="android:spinnerMode">dropdown</item>
        <item name="android:gravity">center_horizontal</item>


    </style>

    <!-- Buttons -->
    <style name="buttonTheme">
        <item name="android:layout_width">@dimen/btn_width</item>
        <item name="android:layout_height">@dimen/btn_height</item>
        <item name="android:background">@drawable/button_shape</item>
        <item name="android:textSize">@dimen/btnText</item>
        <item name="android:textColor">@color/dark_red</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:layout_marginTop">25dp</item>
        <item name="android:layout_gravity">center</item>
    </style>

                            <!--Custom Tabs -->
    <style name="CustomTabText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title.Inverse">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:background">@color/dark_red</item>
    </style>



    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Gets rid of the search icon -->
        <item name="searchIcon">@drawable/ic_search</item>
        <!-- Gets rid of the "underline" in the text -->
        <item name="queryBackground">@color/white</item>
        <!-- Gets rid of the search icon when the SearchView is expanded -->
        <item name="searchHintIcon">@null</item>
        <!-- The hint text that appears when the user has not typed anything -->
        <item name="queryHint">@string/search_hint</item>
        <item name="android:background">@drawable/searchbar</item>


    </style>

</resources>
